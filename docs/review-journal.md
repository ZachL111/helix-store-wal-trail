# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its databases focus without claiming live deployment or external usage.

## Cases

- `baseline`: `index fit`, score 168, lane `ship`
- `stress`: `join width`, score 161, lane `ship`
- `edge`: `constraint risk`, score 199, lane `ship`
- `recovery`: `plan drift`, score 204, lane `ship`
- `stale`: `index fit`, score 197, lane `ship`

## Note

The useful failure mode here is a wrong decision on a named case, not a vague style disagreement.
